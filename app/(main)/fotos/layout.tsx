import type { Metadata } from "next";

export const metadata: Metadata = { title: "Foto's — RLP26" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
