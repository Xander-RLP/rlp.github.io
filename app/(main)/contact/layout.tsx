import type { Metadata } from "next";

export const metadata: Metadata = { title: "Contact — RLP26" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
